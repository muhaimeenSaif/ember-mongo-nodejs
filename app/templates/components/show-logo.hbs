{{#if logoIsShowing}}
  <img class="ui centered image" src="/assets/images/UWindsor_Logo_Rev.png" alt="">
  <button class="ui button" {{action "hideLogo"}}>
    Hide Logo
  </button>
{{else}}
  <button class="ui button" {{action "showLogo"}}>
    Show Logo
  </button>
{{/if}}
{{yield}}
